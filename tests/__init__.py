"""ANNA v2 test suite.

Lightweight pytest suite covering the persistence, parsing, scheduling
and loop-helper modules. Tests use temporary directories and in-memory
SQLite databases so they never touch production data.
"""
